{
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "test": "vp run -r test",
    "build": "vp run -r build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "0.1.20"
  },
  "packageManager": "pnpm@10.33.2"
}
