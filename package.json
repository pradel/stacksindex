{
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "test": "vp run -r test",
    "bench": "vp run -r bench",
    "build": "vp run -r build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "0.1.24"
  },
  "packageManager": "pnpm@11.24.0"
}
