---
"stacksindex": minor
---

Use the new `GET /extended/v3/transactions/batch` endpoint (Stacks API 9.2.0) to fetch missing transactions in batches of up to 20 with a single request instead of one request per transaction.
